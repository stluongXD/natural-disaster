# global enviroment where all files have access to
homicide_circumstance <- read.csv(
  "data/homicide_circumstance.csv",
  stringsAsFactors = F
)
case_info <- read.csv("data/case_info.csv", stringsAsFactors = F)
